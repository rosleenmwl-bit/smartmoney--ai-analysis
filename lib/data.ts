import { createClient } from "@/lib/supabase/server";

const DEFAULT_BUDGETS: Record<string, number> = { "Home Loan Repayment": 1500, "Unifi Home Broadband": 149, "Mobile Phone Lines": 120, "2nd Home Utilities & Maintenance": 350, "Restaurant Meals": 600, "Online Shopping": 500, "Medical Expenses": 300, "Grocery Shopping": 800, "Car Expenses": 500, "Ad-Hoc Expenses": 1181 };
const DEMO_CATEGORIES = [{ id: "02cae5cb-1178-49b3-8fea-c3e0e2b2a4e8", name: "Home Loan Repayment", type: "fixed", icon: "🏠", sort_order: 1 }, { id: "5d675bcd-74f3-4c8c-be5e-95ab64e232e2", name: "Unifi Home Broadband", type: "fixed", icon: "🌐", sort_order: 2 }, { id: "38f1cf87-4965-4512-8077-a05c0c437283", name: "Mobile Phone Lines", type: "fixed", icon: "📱", sort_order: 3 }, { id: "3fa19c3b-4e24-4fb7-8625-302ba18f4b6f", name: "2nd Home Utilities & Maintenance", type: "fixed", icon: "🔑", sort_order: 4 }, { id: "7d780cfe-ffa3-4851-b8bf-76d20d84a008", name: "Restaurant Meals", type: "variable", icon: "🍽️", sort_order: 5 }, { id: "c3ed1e27-399c-436e-9f1c-b3492dac87ce", name: "Online Shopping", type: "variable", icon: "🛒", sort_order: 6 }, { id: "81cd3188-c5af-45e1-b3e8-4e317e53a72d", name: "Medical Expenses", type: "variable", icon: "🏥", sort_order: 7 }, { id: "d52f7158-fc32-4cfb-8a35-9011c54dae9e", name: "Grocery Shopping", type: "variable", icon: "🥦", sort_order: 8 }, { id: "94953623-2d1a-4b4a-ba04-28c9ebb7ed56", name: "Car Expenses", type: "variable", icon: "🚗", sort_order: 9 }, { id: "62316372-c8ed-4c79-bcb9-9ecd2675d656", name: "Ad-Hoc Expenses", type: "variable", icon: "📦", sort_order: 10 }];
function buildDemoDashboard(month: string) { const categories = DEMO_CATEGORIES; const budgets = categories.map((c) => ({ id: `demo-budget-${c.id}`, month, category_id: c.id, allocated_amount: DEFAULT_BUDGETS[c.name], is_total: false })); budgets.push({ id: "demo-budget-total", month, category_id: "", allocated_amount: 6000, is_total: true }); const expenses = [{ id: "demo-1", category_id: DEMO_CATEGORIES[0].id, amount: 1500, expense_date: `${month}-01`, capture_method: "manual", note: "Monthly home loan", categories: DEMO_CATEGORIES[0] }, { id: "demo-2", category_id: DEMO_CATEGORIES[1].id, amount: 149, expense_date: `${month}-01`, capture_method: "manual", note: "Broadband", categories: DEMO_CATEGORIES[1] }, { id: "demo-3", category_id: DEMO_CATEGORIES[2].id, amount: 120, expense_date: `${month}-01`, capture_method: "manual", note: "Mobile phone lines", categories: DEMO_CATEGORIES[2] }, { id: "demo-4", category_id: DEMO_CATEGORIES[3].id, amount: 350, expense_date: `${month}-01`, capture_method: "manual", note: "2nd home utilities and maintenance", categories: DEMO_CATEGORIES[3] }, { id: "demo-5", category_id: DEMO_CATEGORIES[4].id, amount: 180, expense_date: `${month}-07`, capture_method: "manual", note: "Family dinner", categories: DEMO_CATEGORIES[4] }, { id: "demo-6", category_id: DEMO_CATEGORIES[5].id, amount: 95, expense_date: `${month}-10`, capture_method: "manual", note: "Household items", categories: DEMO_CATEGORIES[5] }, { id: "demo-7", category_id: DEMO_CATEGORIES[7].id, amount: 260, expense_date: `${month}-12`, capture_method: "manual", note: "Weekly groceries", categories: DEMO_CATEGORIES[7] }, { id: "demo-8", category_id: DEMO_CATEGORIES[8].id, amount: 120, expense_date: `${month}-15`, capture_method: "manual", note: "Petrol and tolls", categories: DEMO_CATEGORIES[8] }, { id: "demo-9", category_id: DEMO_CATEGORIES[6].id, amount: 85, expense_date: `${month}-18`, capture_method: "manual", note: "Pharmacy", categories: DEMO_CATEGORIES[6] }, { id: "demo-10", category_id: DEMO_CATEGORIES[9].id, amount: 60, expense_date: `${month}-20`, capture_method: "manual", note: "Small repairs", categories: DEMO_CATEGORIES[9] }]; const spent = expenses.reduce((s, e) => s + e.amount, 0); const fixedSpent = expenses.filter((expense) => expense.categories.type === "fixed").reduce((sum, expense) => sum + expense.amount, 0); const comparisons = categories.map((c) => { const budget = DEFAULT_BUDGETS[c.name]; const actual = expenses.filter((e) => e.category_id === c.id).reduce((s, e) => s + e.amount, 0); return { ...c, budget, actual, residual: budget - actual }; }); return { month, categories, budgets, expenses, recommendation: { id: "demo-recommendation", suggestion_text: "Restaurant Meals is your largest flexible category. Try one planned home-cooked meal each week and keep the social meals you enjoy.", primary_idea: "Reduce one restaurant meal per week.", next_actions: ["Plan two simple meals at home", "Review dining spend on Sunday", "Move the saved amount to your buffer"] }, allocated: 6000, spent, balance: 6000 - spent, fixedAllocated: 2119, fixedSpent, flexibleSpent: spent - fixedSpent, comparisons, weekly: expenses.filter((e) => e.categories.type !== "fixed").slice(-3).reverse(), weekStart: `${month}-24`, previewMode: true }; }
function monthKey(date = new Date()) { return date.toISOString().slice(0, 7); }
function secureMode() { return process.env.SMARTMONEY_AUTH_REQUIRED === "true"; }
function startOfWeek(date = new Date()) { const d = new Date(date); const day = d.getDay(); d.setDate(d.getDate() - (day === 0 ? 6 : day - 1)); return d.toISOString().slice(0, 10); }
function topFlexibleCategories(comparisons: any[]) { return comparisons.filter((c) => c.type !== "fixed" && c.actual > 0).sort((a, b) => b.actual - a.actual).slice(0, 3).map((c) => ({ id: `top-${c.id}`, amount: c.actual, expense_date: "", capture_method: "category total", note: "Highest flexible spend", categories: c })); }
function includeSavedReceipt(d: any, month: string) { if (month !== "2026-08" || d.expenses.some((e: any) => e.note === "The Dining Room")) return d; const category = DEMO_CATEGORIES.find((c) => c.name === "Restaurant Meals")!; const amount = 237.65; d.expenses.push({ id: "saved-receipt-2026-08-29", category_id: category.id, amount, expense_date: "2026-08-29", capture_method: "receipt", note: "The Dining Room", categories: category }); d.spent += amount; d.balance -= amount; d.flexibleSpent += amount; d.comparisons = d.comparisons.map((c: any) => c.id === category.id ? { ...c, actual: c.actual + amount, residual: c.residual - amount } : c); return d; }

export async function ensureMonth(month = monthKey()) {
  const supabase = await createClient();
  const { data: cats, error: categoryError } = await supabase.from("categories").select("id,name,type,icon,sort_order").order("sort_order");
  if (categoryError) {
    if (secureMode()) throw new Error("Unable to load categories from the database.");
    return { month, categories: DEMO_CATEGORIES, previewMode: true };
  }
  if (!cats?.length) {
    if (secureMode()) throw new Error("No categories are available for this account.");
    return { month, categories: DEMO_CATEGORIES, previewMode: true };
  }
  const { data: existing } = await supabase.from("budgets").select("id").eq("month", month).limit(1);
  if (!existing?.length) { await supabase.from("budgets").insert(cats.map((c) => ({ month, category_id: c.id, allocated_amount: DEFAULT_BUDGETS[c.name] ?? 0, is_total: false }))); await supabase.from("budgets").insert({ month, allocated_amount: 6000, is_total: true }); }
  const { data: expenses } = await supabase.from("expenses").select("id").gte("expense_date", `${month}-01`).lt("expense_date", `${month}-32`).limit(1);
  const byName = Object.fromEntries(cats.map((c) => [c.name, c.id]));
  const { data: fixedRows } = await supabase.from("expenses").select("category_id").gte("expense_date", `${month}-01`).lt("expense_date", `${month}-32`).in("category_id", cats.filter((category) => category.type === "fixed").map((category) => category.id));
  const fixedIds = new Set((fixedRows ?? []).map((row) => row.category_id));
  const missingFixed = cats.filter((category) => category.type === "fixed" && !fixedIds.has(category.id));
  if (missingFixed.length) await supabase.from("expenses").insert(missingFixed.map((category) => ({ category_id: category.id, amount: DEFAULT_BUDGETS[category.name] ?? 0, expense_date: `${month}-01`, capture_method: "manual", note: `Monthly ${category.name}` })).filter((expense) => expense.amount > 0));
  if (!expenses?.length) { await supabase.from("expenses").insert([["Restaurant Meals", 85, "Family dinner"], ["Restaurant Meals", 42, "Lunch with friend"], ["Grocery Shopping", 230, "Weekly groceries"], ["Car Expenses", 60, "Petrol refill"]].map(([name, amount, note], i) => ({ category_id: byName[name as string], amount, expense_date: `${month}-${String(Math.min(27, 6 + i * 4)).padStart(2, "0")}`, capture_method: "manual", note })) ); }
  const { data: recs } = await supabase.from("recommendations").select("id").eq("month", month).limit(1);
  if (!recs?.length && byName["Restaurant Meals"]) await supabase.from("recommendations").insert({ month, top_reducible_category_id: byName["Restaurant Meals"], suggestion_text: "Restaurant Meals is a flexible category to watch. Try one planned home-cooked meal each week and keep the social meals you enjoy.", primary_idea: "Reduce one restaurant meal per week — a small change with a visible monthly effect.", backup_idea: "Set a weekly dining-out envelope and stop when it is used.", risks_text: "Keep room for social enjoyment and essential groceries; adjust gently.", next_actions: ["Plan two simple meals at home", "Review dining spend on Sunday", "Move the saved amount to your buffer"], source: "rule-based", confidence: 0.8, review_status: "unreviewed" });
  return { month, categories: cats };
}

export async function getDashboard(month = monthKey()) {
  const supabase = await createClient(); const { categories } = await ensureMonth(month);
  const [budgetRes, expenseRes, recRes] = await Promise.all([supabase.from("budgets").select("id,month,category_id,allocated_amount,is_total").eq("month", month), supabase.from("expenses").select("id,category_id,amount,expense_date,capture_method,note,created_at,categories(name,type,icon)").gte("expense_date", `${month}-01`).lt("expense_date", `${month}-32`).order("expense_date", { ascending: false }), supabase.from("recommendations").select("*,categories(name,icon)").eq("month", month).order("created_at", { ascending: false }).limit(1)]);
  if (budgetRes.error || expenseRes.error || !categories.length) {
    if (secureMode()) throw new Error("Unable to load your spending data from the database.");
    return includeSavedReceipt(buildDemoDashboard(month), month);
  }
  const budgets = budgetRes.data ?? []; const expenses = expenseRes.data ?? []; const recommendation = recRes.data?.[0] ?? null;
  const allocated = Number(budgets.find((b) => b.is_total)?.allocated_amount ?? budgets.filter((b) => !b.is_total).reduce((s, b) => s + Number(b.allocated_amount), 0)); const spent = expenses.reduce((s, e) => s + Number(e.amount), 0);
  const comparisons = categories.map((c) => { const budget = Number(budgets.find((b) => b.category_id === c.id && !b.is_total)?.allocated_amount ?? 0); const actual = expenses.filter((e) => e.category_id === c.id).reduce((s, e) => s + Number(e.amount), 0); return { ...c, budget, actual, residual: budget - actual }; });
  const fixedAllocated = comparisons.filter((c) => c.type === "fixed").reduce((s, c) => s + c.budget, 0);
  const fixedSpent = comparisons.filter((c) => c.type === "fixed").reduce((s, c) => s + c.actual, 0);
  const weekly = expenses.filter((e) => (e.categories as any)?.type !== "fixed").sort((a, b) => Number(b.amount) - Number(a.amount)).slice(0, 3);
  return { month, categories, budgets, expenses, recommendation, allocated, spent, balance: allocated - spent, fixedAllocated, fixedSpent, flexibleSpent: spent - fixedSpent, comparisons, weekly, weekStart: startOfWeek() };
}
export async function getCategories() { const supabase = await createClient(); const { data } = await supabase.from("categories").select("id,name,type,icon").order("sort_order"); return data ?? []; }
