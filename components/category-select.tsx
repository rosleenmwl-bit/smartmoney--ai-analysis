export default function CategorySelect({ categories, name = "category_id", initialValue = "" }: { categories: any[]; name?: string; initialValue?: string }) { const fixed = categories.filter((c) => c.type === "fixed"); const flexible = categories.filter((c) => c.type !== "fixed"); return <select name={name} defaultValue={initialValue} required><option value="" disabled>Select category</option><optgroup label="Fixed expenses">{fixed.map((c) => <option key={c.id} value={c.id}>{c.icon} {c.name}</option>)}</optgroup><optgroup label="Flexible expenses">{flexible.map((c) => <option key={c.id} value={c.id}>{c.icon} {c.name}</option>)}</optgroup></select> }
