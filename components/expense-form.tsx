"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { createExpense } from "@/app/actions";
export default function ExpenseForm({ categories, compact = false }: { categories: any[]; compact?: boolean }) { const router = useRouter(); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setError(""); setSaving(true); try { await createExpense(new FormData(e.currentTarget)); e.currentTarget.reset(); router.refresh(); } catch (e) { setError(e instanceof Error ? e.message : "Could not save expense."); } finally { setSaving(false); } } return <form className={`expense-form ${compact ? "compact" : ""}`} onSubmit={submit}><div className="form-grid"><label>Amount (MYR)<input name="amount" type="number" min="0.01" step="0.01" placeholder="150.00" required /></label><label>Category<select name="category_id" required defaultValue=""><option value="" disabled>Select category</option>{categories.map((c) => <option key={c.id} value={c.id}>{c.icon} {c.name}</option>)}</select></label><label>Date<input name="expense_date" type="date" defaultValue={new Date().toISOString().slice(0, 10)} required /></label><label className="wide">Note (optional)<input name="note" placeholder="Lunch with family" /></label></div>{error && <p className="form-error">{error}</p>}<button className="button primary" disabled={saving}>{saving ? "Saving…" : "Save expense"}</button></form> }
