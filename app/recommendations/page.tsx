import Link from "next/link";
import { getDashboard } from "@/lib/data";
import { generateRecommendation, updateRecommendation } from "@/app/actions";
export const dynamic = "force-dynamic";
export default async function RecommendationsPage() { const d = await getDashboard(); const r = d.recommendation; return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">S</span><div><strong>SmartMoney</strong><small>AI budget coach</small></div></div><nav><Link href="/">◈ <span>Dashboard</span></Link><Link href="/expenses">↗ <span>Expenses</span></Link><Link href="/budget">▣ <span>Budget</span></Link><Link className="active" href="/recommendations">✦ <span>Recommendations</span></Link></nav></aside><main className="main-content"><header className="topbar"><div><p className="eyebrow">Savings coach</p><h1>Small changes, more breathing room.</h1></div><Link className="button secondary" href="/">← Dashboard</Link></header><section className="card recommendation-large"><div className="section-heading"><div><p className="eyebrow">{d.month} recommendation</p><h2>Practical next steps</h2></div><span className="sparkle">✦</span></div>{r ? <><p className="recommendation-text">{r.suggestion_text}</p><div className="ideas-grid"><div className="idea"><small>PRIMARY IDEA</small><p>{r.primary_idea}</p></div><div className="idea"><small>BACKUP IDEA</small><p>{r.backup_idea}</p></div><div className="idea"><small>RISKS & ADJUSTMENTS</small><p>{r.risks_text}</p></div></div><div className="next-actions"><h3>Next three actions this week</h3>{(r.next_actions ?? []).map((a: string, i: number) => <div key={a}><span>{i + 1}</span>{a}</div>)}</div><div className="action-row"><form action={updateRecommendation.bind(null, r.id, "accepted")}><button className="button primary">Accept recommendation</button></form><form action={updateRecommendation.bind(null, r.id, "dismissed")}><button className="button secondary">Dismiss</button></form></div></> : <div className="empty-state"><h3>Need at least 5 expenses this month to generate recommendations.</h3><p>Add a few more expenses, then come back for a tailored savings plan.</p><form action={generateRecommendation}><button className="button primary">Generate recommendation</button></form></div>}</section></main></div> }
